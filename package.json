{
  "name": "kana-beat",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run prepare-model && vite",
    "prepare-model": "node scripts/prepare-vosk-model.mjs",
    "build": "npm run prepare-model && vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "vosk-browser": "0.0.8"
  },
  "devDependencies": {
    "extract-zip": "^2.0.1",
    "tar": "^7.5.20",
    "vite": "8.1.4"
  }
}
